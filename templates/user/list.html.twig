{% extends 'base.html.twig' %}

{% block body %}

    <h1>Users</h1>

    <p><a href="{{ path('users.register') }}">Register new</a></p>

    <ul>
        {% for user in users %}
        <li>{{ user.name }} &lt;{{ user.email }}&gt;</li>
        {% endfor %}
    </ul>
{% endblock %}

